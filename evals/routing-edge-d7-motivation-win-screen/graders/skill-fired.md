---
type: tool_use
ablation: with-only
---

The answer must come from `game-engagement-retention-skills:interaction-reward-moments`.

PASS when interaction-reward-moments fired alone.
FAIL when retention-strategy-designer fired (the tempting answer), when the advisor fired, or when no plugin skill fired.

The tempting wrong route: "D7" is a lifecycle metric and reads as a retention ask. It is cited only as the motivation; the deliverable named is one in-session screen. The rule under test is "a retention metric cited only as motivation or as a success criterion is NOT a second ask."

With-only — fired/not-fired indicator, not part of the score delta.
