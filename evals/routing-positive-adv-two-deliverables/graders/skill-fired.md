---
type: tool_use
ablation: with-only
---

The answer must come from `game-engagement-retention-skills:engagement-retention-advisor`, invoked via the Skill tool.

PASS when engagement-retention-advisor fired and neither sibling skill fired.
FAIL when interaction-reward-moments or retention-strategy-designer fired instead, when both siblings fired in sequence instead of the advisor, or when no plugin skill fired.

Why this is the clean positive: two named deliverables sit in one request — an in-session reveal beat AND a multi-day return plan — which is the advisor's defining trigger ("Two deliverables, or the moment-to-return link itself").

With-only. The baseline arm cannot fire a skill that is not installed; read this as a fired/not-fired indicator only.
