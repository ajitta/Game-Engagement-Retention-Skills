---
type: tool_use
ablation: with-only
---

The answer must come from `game-engagement-retention-skills:engagement-retention-advisor`.

PASS when engagement-retention-advisor fired alone.
FAIL when interaction-reward-moments or retention-strategy-designer fired (each owns one of the two options being compared), when both fired, or when no plugin skill fired.

The tempting wrong route: the request names one moment-level and one lifecycle-level investment, so whichever one the model reads first looks like the skill. The deliverable is neither option — it is the comparison, which is the advisor's `compare` row ("Compare / sequence / prioritize moment-level vs lifecycle-level").

With-only — fired/not-fired indicator only.
