---
type: tool_use
ablation: with-only
---

The answer must come from `game-engagement-retention-skills:retention-strategy-designer`.

PASS when retention-strategy-designer fired alone.
FAIL when no plugin skill fired (v1's live routing miss #26), or when a sibling fired.

Homeless case: an event taxonomy reads as engineering work rather than retention design, so v1 routed it nowhere. Instrumentation is what makes every later retention claim checkable, and it is this skill's row.

With-only — fired/not-fired indicator only.
