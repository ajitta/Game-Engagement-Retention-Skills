---
type: tool_use
ablation: with-only
---

The answer must come from `game-engagement-retention-skills:interaction-reward-moments`.

PASS when interaction-reward-moments fired alone.
FAIL when retention-strategy-designer fired (the tempting answer — session length reads as an engagement KPI), when the advisor fired, or when no plugin skill fired.

Secondary assertion, same case: the answer must treat session length as a diagnostic of loop satisfaction and must not accept "raise median session length" as the target. If the answer sets session length as the metric to move, FAIL even when the right skill fired.

The tempting wrong route: "too short" is phrased as a number to raise, which pulls toward the lifecycle skill. A quit-mid-session complaint is an in-session loop symptom.

With-only — fired/not-fired indicator only.
